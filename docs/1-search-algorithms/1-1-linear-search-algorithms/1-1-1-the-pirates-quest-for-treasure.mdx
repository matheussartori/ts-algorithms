---
sidebar_position: 1
---

import { Editor } from '../../../src/algorithms/editor/editor'

# 1. The Pirate's Quest for Treasure

![The Pirate's Quest for Treasure](./img/1-1-1.png)

In this pirate adventure, Captain Tom buried his treasure behind trees with a certain number of branches, as indicated
on the treasure map. However, on this island, each tree is unique in terms of the number of branches it has. Given an
array representing the number of branches on each tree, locate the tree with the specified number of branches.

If the specified number of branches is not found, return -1.

```typescript
const branchesOnTrees = [2, 4, 6, 8, 10]
const targetBranchCount = 6
findTreeIndexByBranches(branchesOnTrees, targetBranchCount)
// Expected Output: Index of tree with 6 branches (2)
```

<Editor
  editorHeight={150}
  args="[2, 4, 6, 8, 10], 6"
  expectedResult="2"
  tests={[
    {
      input: '[2, 4, 6, 8, 10, 12, 14], 6',
      output: 2
    },
    {
      input: '[1, 3, 5, 7, 9, 11], 13',
      output: -1
    },
    {
      input: '[10, 20, 30, 40, 50, 60, 70], 40',
      output: 3
    },
    {
      input: '[100, 200, 300, 400, 500, 600], 250',
      output: -1
    },
    {
      input: '[7, 14, 21, 28, 35, 42], 21',
      output: 2
    },
    {
      input: '[8, 16, 24, 32, 40, 48], 35',
      output: -1
    }
  ]}
  initialCode={`function findTreeIndexByBranches (branchesOnTrees: number[], targetBranchCount: number): number {
  return -1
}`} />
